<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/fomes_content_background_dark">

    <include android:id="@+id/recommend_contents_layout"
        layout="@layout/fragment_recommend_contents"
        android:visibility="gone"/>

    <include android:id="@+id/recommend_error_layout"
        layout="@layout/fragment_recommend_error"
        android:visibility="gone"/>

    <ProgressBar android:id="@+id/recommend_loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"/>

</FrameLayout>